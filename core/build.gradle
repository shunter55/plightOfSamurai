apply plugin: "java"

sourceCompatibility = 1.8
dependencies {
    compile project(path: ':GameFramework')
}

[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]
